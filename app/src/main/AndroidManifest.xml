<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nguyen.hoang.movierating" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="nguyen.hoang.movierating.ParseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MaterialTheme">

        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="M9fyrnb1FAAFzAItUcyxjpXisc4wHryXn44qw6n4" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="RvDIQl4w0vmMCsOHZGkbZGSppemZ8Z05S78QAzG2" />

        <uses-sdk android:targetSdkVersion="16" />

        <activity
            android:name=".AccountManagement.LaunchActivity"
            android:label="@string/title_activity_launch"
            android:theme="@style/MaterialTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MovieRating.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/MaterialTheme">
        </activity>
    </application>

</manifest>
